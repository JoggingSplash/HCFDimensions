{
    "name": "bit/hcfdimensions",
    "description": "A plugin for dimensions",
    "type": "project",
    "require": {},
    "scripts": {
        "make-plugin":  [
        "@composer install --no-dev --classmap-authoritative --ignore-platform-reqs",
        "@php -dphar.readonly=0 ../HardCoreFactions/make-phar.php"
        ]
    }
}
